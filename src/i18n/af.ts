import { en } from './en';

/** Afrikaans scaffold. Kept unavailable until native-speaker review is complete. */
export const af: typeof en = { ...en, LANGUAGE_LABEL: 'Taal', SPEECH_LOCALE: 'af-ZA' };
