import { en } from './en';

/** isiXhosa scaffold. Kept unavailable until native-speaker review is complete. */
export const xh: typeof en = { ...en, LANGUAGE_LABEL: 'Ulwimi', SPEECH_LOCALE: 'xh-ZA' };
