import { en } from './en';

/** Amharic scaffold. Kept unavailable until translation and cheap-device font review are complete. */
export const am: typeof en = { ...en, LANGUAGE_LABEL: 'ቋንቋ', SPEECH_LOCALE: 'am-ET' };
