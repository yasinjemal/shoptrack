import { en } from './en';

/** Swahili scaffold. Kept unavailable until native-speaker review is complete. */
export const sw: typeof en = { ...en, LANGUAGE_LABEL: 'Lugha', SPEECH_LOCALE: 'sw-KE' };
